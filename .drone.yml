image: weyforth/drone-docker
script:
  - tools/test
deploy:
  ssh:
    target: deploy@46.101.50.39:app-build/
    artifacts:
      - .
    cmd: app-build/tools/deploy
    when:
      branch: docker
notify:
  email:
    recipients:
      - build@mikefarrow.co.uk