#!/bin/bash

set -e

build_env="local"

if [[ "$1" != "" ]];
	then
	build_env="$1"
fi

cp ".env.$build_env" .env.example

exec tools/composer install
exec tools/npm install
exec tools/bower install

tools/grunt build
