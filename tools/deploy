#!/bin/bash

source app-build/tools/_functions

set -e

if [[ -d app ]]; then

	cd app

	heading "Stopping"
	tools/stop production

	cd ../

fi

heading "Copying files"

rm -rf app
cp -r app-build app

cd app

heading "Starting"
tools/start production

tools/install production

echo ""

# mkdir -p app-shared
# mkdir -p app-shared/logs

# rm -rf app/logs/
# ln -s app-shared/logs/ app/logs
